// Utiliza la funcion "every" para comprobar 
// que todos los elementos de un array son pares

let arr = [1, 2, 3, 4, 5];
console.log(arr.every(num => num % 2 == 0))// false Mira los pares y no hay

let arr1 = [2, 4, 6, 8 ,10 ,12];
console.log(arr1.every(num => num % 2 == 0))// false Mira los pares y no hay